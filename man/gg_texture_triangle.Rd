% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_texture_triangle.R
\name{gg_texture_triangle}
\alias{gg_texture_triangle}
\title{Plot a soil texture triangle with sample points}
\usage{
gg_texture_triangle(
  data,
  sand,
  silt,
  clay,
  colour = NULL,
  system = "USDA",
  style = c("none", "wiki"),
  breaks = c(20, 40, 60, 80),
  grid_colour = "grey70",
  grid_linetype = "dashed",
  grid_linewidth = 0.25,
  border_colour = "grey40",
  border_linewidth = 0.35,
  tick_height = NULL,
  surface = NULL,
  label_style = list()
)
}
\arguments{
\item{data}{A data frame or tibble containing sample points to plot.}

\item{sand}{Bare column name (or scalar) for sand percentage (0–100).}

\item{silt}{Bare column name (or scalar) for silt percentage (0–100).}

\item{clay}{Bare column name (or scalar) for clay percentage (0–100).}

\item{colour}{Optional bare column name to map to point colour. Pass \code{NULL}
(the default) for no colour mapping.}

\item{system}{Classification system; currently only \code{"USDA"} is supported.}

\item{style}{Fill colour style for texture class polygons. \code{"none"} (default)
draws unfilled polygons; \code{"wiki"} applies the colour scheme used in the
Wikipedia USDA soil texture diagram.}

\item{breaks}{Numeric vector of percentage values at which to draw grid lines
and tick marks (values between 1 and 99). Defaults to \code{c(20, 40, 60, 80)}.
Common alternatives: \code{c(25, 50, 75)} (quarters), \code{seq(10, 90, 10)} (deciles),
\code{seq(5, 95, 5)} (5 \% steps).}

\item{grid_colour}{Colour of the internal grid lines. Defaults to \code{"grey70"}.}

\item{grid_linetype}{Line type of the internal grid lines. Defaults to
\code{"dashed"}. Any value accepted by \code{\link[ggplot2:geom_path]{ggplot2::geom_line()}} is valid
(e.g. \code{"solid"}, \code{"dotted"}, \code{"blank"} to hide the grid entirely).}

\item{grid_linewidth}{Stroke width of the internal grid lines. Defaults to
\code{0.25}.}

\item{border_colour}{Colour of the texture class boundary lines. Defaults to
\code{"grey40"}. Set to \code{NA} to hide all class borders.}

\item{border_linewidth}{Stroke width of the class boundary lines. Defaults
to \code{0.35}.}

\item{tick_height}{Height of the tick marks drawn on each triangle edge, in
Cartesian coordinate units. Defaults to \code{NULL}, which auto-scales with
break density. Set to \code{0} to hide all tick marks while keeping grid lines
and labels.}

\item{surface}{Optional. A \code{"texture_surface"} object produced by
\code{\link[=texture_surface]{texture_surface()}}, specifying a numeric variable to interpolate and
display as a continuous fill gradient across the triangle. When supplied,
any \code{style} palette fill is suppressed in favour of the surface; the fill
scale is left unset so the user can control it freely with
\verb{+ ggplot2::scale_fill_*()}.\cr
For iso-contour lines on top of the surface (or on a plain triangle), see
\code{\link[=geom_texture_contour]{geom_texture_contour()}}.}

\item{label_style}{Named list of text styling overrides. Prefer building
this with \code{\link[=texture_label_style]{texture_label_style()}} for argument completion and validation.
Recognised keys:
\describe{
\item{\code{tick_show}}{Show tick-mark percentage labels (default \code{TRUE}).}
\item{\code{tick_size}}{Font size of tick labels. Defaults to auto-scale with
break density (2.2 / 1.8 / 1.4 for ≤5 / ≤10 / >10 breaks).}
\item{\code{axis_show}}{Show axis name labels (default \code{TRUE}).}
\item{\code{axis_size}}{Font size of axis name labels (default \code{3.2}).}
\item{\code{class_show}}{Show texture class name labels inside polygons
(default \code{TRUE}).}
\item{\code{class_size}}{Font size of texture class labels (default \code{2.5}).}
}
Only the keys you supply are overridden; unspecified keys use defaults.}
}
\value{
A \code{ggplot} object.
}
\description{
Produces a \code{ggplot2} object displaying a soil texture triangle with each
texture class region drawn as a filled polygon and optional sample points
overlaid. Triangle geometry is built from \link{usda_texture_classes} using
an internal ternary-to-Cartesian coordinate conversion.
}
\examples{
soils <- tibble::tibble(
  sand = c(70, 20, 40, 10),
  silt = c(15, 30, 40, 20),
  clay = c(15, 50, 20, 70)
)

# Defaults
gg_texture_triangle(soils, sand = sand, silt = silt, clay = clay)

# Wiki colours, 10 \% grid, no class labels
gg_texture_triangle(soils, sand, silt, clay,
  style = "wiki", breaks = seq(10, 90, 10),
  label_style = list(class_show = FALSE))

# Solid grid in dark grey, hide tick labels
gg_texture_triangle(soils, sand, silt, clay,
  grid_colour = "grey40", grid_linetype = "solid",
  label_style = list(tick_show = FALSE))
}
