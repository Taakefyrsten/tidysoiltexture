% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/texture_surface.R
\name{geom_texture_contour}
\alias{geom_texture_contour}
\title{Add iso-contour lines to a texture triangle plot}
\usage{
geom_texture_contour(
  data,
  sand,
  silt,
  clay,
  z,
  breaks = NULL,
  resolution = 150L,
  power = 2,
  ...
)
}
\arguments{
\item{data}{A data frame containing sample points.}

\item{sand, silt, clay}{Bare column names for the ternary coordinates (0–100).}

\item{z}{Bare column name for the numeric value to interpolate.}

\item{breaks}{Numeric vector of z values at which to draw contour lines.
Passed to \code{\link[ggplot2:geom_contour]{ggplot2::geom_contour()}}. If \code{NULL} (default), ggplot2 chooses
breaks automatically.}

\item{resolution}{Integer. Interpolation grid width (default \code{150L}).}

\item{power}{Positive numeric. IDW exponent (default \code{2}).}

\item{...}{Additional arguments passed to \code{\link[ggplot2:geom_contour]{ggplot2::geom_contour()}}
(e.g. \code{linewidth}, \code{colour}, \code{linetype}).}
}
\value{
A \code{geom_contour} ggplot2 layer.
}
\description{
Interpolates a numeric variable from scattered ternary sample points onto a
regular Cartesian grid (using IDW) and draws contour lines at the specified
values. Add to a plot produced by \code{\link[=gg_texture_triangle]{gg_texture_triangle()}} with \code{+}.
}
\details{
Contour lines are rendered on top of all triangle layers, making them
suitable for highlighting analytical thresholds (e.g. a significance
boundary at \code{p = 0.05}).
}
\examples{
set.seed(7)
surf_df <- data.frame(
  sand = runif(80, 5, 90),
  clay = runif(80, 5, 60)
) |>
  dplyr::mutate(silt = 100 - sand - clay) |>
  dplyr::filter(silt >= 0) |>
  dplyr::mutate(p_val = pnorm(scale(clay)[, 1]))

pts <- data.frame(sand = c(40, 20), silt = c(40, 30), clay = c(20, 50))

gg_texture_triangle(pts, sand, silt, clay) +
  geom_texture_contour(surf_df, sand, silt, clay, z = p_val,
                       breaks = 0.05, linewidth = 1.4, colour = "red")
}
