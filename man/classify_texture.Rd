% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_texture.R,
%   R/classify_texture_spatial.R
\name{classify_texture}
\alias{classify_texture}
\alias{classify_texture.default}
\alias{classify_texture.sf}
\alias{classify_texture.SpatRaster}
\title{Classify soil texture from sand, silt, and clay percentages}
\usage{
classify_texture(data, sand, silt, clay, system = "USDA", ...)

\method{classify_texture}{default}(data, sand, silt, clay, system = "USDA", ...)

\method{classify_texture}{sf}(data, sand, silt, clay, system = "USDA", ...)

\method{classify_texture}{SpatRaster}(
  data,
  sand = "sand",
  silt = "silt",
  clay = "clay",
  system = "USDA",
  ...
)
}
\arguments{
\item{data}{A data frame / tibble, an \code{sf} object, or a \code{SpatRaster}.}

\item{sand}{For data frames and \code{sf}: bare column name (or scalar) giving
sand percentage (0–100). For \code{SpatRaster}: character layer name
(default \code{"sand"}).}

\item{silt}{As \code{sand}, for silt percentage.}

\item{clay}{As \code{sand}, for clay percentage.}

\item{system}{Classification system. Currently only \code{"USDA"} is supported.}

\item{...}{Reserved for future use.}
}
\value{
\itemize{
\item \strong{data frame / tibble}: the input with two appended columns,
\code{.texture_class} and \code{.texture_abbr}.
\item \strong{sf}: the input \code{sf} object with the same two columns appended,
geometry preserved.
\item \strong{SpatRaster}: a single-layer categorical \code{SpatRaster} named
\code{"texture_class"}, with integer cell values mapped to class names via
\code{terra::levels()}.
}
}
\description{
Classifies each row of \code{data} into a USDA soil texture class. Dispatches
on the class of \code{data}, so the same function works with plain data frames
or tibbles, \code{sf} point objects, and \code{terra} \code{SpatRaster} stacks.
}
\examples{
# --- data frame / tibble -------------------------------------------------
soils <- data.frame(
  sand = c(70, 20, 40, 10),
  silt = c(15, 30, 40, 20),
  clay = c(15, 50, 20, 70)
)
classify_texture(soils, sand = sand, silt = silt, clay = clay)

# --- sf point object -----------------------------------------------------
if (requireNamespace("sf", quietly = TRUE)) {
  pts <- data.frame(
    sand = c(70, 20, 40, 10),
    silt = c(15, 30, 40, 20),
    clay = c(15, 50, 20, 70),
    lon  = c(10.1, 10.2, 10.3, 10.4),
    lat  = c(59.1, 59.2, 59.3, 59.4)
  )
  pts_sf <- sf::st_as_sf(pts, coords = c("lon", "lat"), crs = 4326)
  classify_texture(pts_sf, sand = sand, silt = silt, clay = clay)
}

# --- terra SpatRaster stack ----------------------------------------------
if (requireNamespace("terra", quietly = TRUE)) {
  set.seed(1)
  n_cells <- 100L
  sand_v  <- runif(n_cells, 5, 65)
  clay_v  <- runif(n_cells, 5, 30)   # sand + clay always < 100
  silt_v  <- 100 - sand_v - clay_v
  r <- terra::rast(ncols = 10, nrows = 10, nlyrs = 3,
                   xmin = 0, xmax = 1, ymin = 0, ymax = 1)
  names(r) <- c("sand", "silt", "clay")
  terra::values(r) <- cbind(sand_v, silt_v, clay_v)
  classify_texture(r, sand = "sand", silt = "silt", clay = "clay")
}
}
